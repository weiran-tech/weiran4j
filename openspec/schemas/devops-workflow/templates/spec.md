<!--
  **本注释只留骨架与格式。判定口径与「为什么」在 `<rules>` 块里** ——
  它与本模板在**同一个提示词内**(CLI 把 config.yaml 的 rules、schema.yaml 的 instruction
  和本模板一起拼给你),所以这不是「跨文件指针」那种失效模式。
  规则只在 rules 里展开一次,这里不再抄第二遍 —— 两份必然漂移。

  骨架(照抄填空,不要自由造句):

    ### Requirement: [FR-NNN] <名词短语,不写 MUST>     ← 正好 3 个 `#`

    <主体> **MUST** <行为>。                            ← 规范陈述在正文,不在标题

    #### Scenario: <名称>                               ← 正好 4 个 `#`
    - **WHEN** <触发条件>
    - **THEN** <预期结果>
    - **判据**:<可观测的判定方式>

  · 每个需求至少配一个场景。层级写错会被静默忽略,archive 合并时才丢内容。
  · 非功能需求(性能、安全、可观测、降级)同样写成 Requirement + Scenario,
    不要塞进散文段落 —— 否则 L8 无法逐条核对。
-->

<!--
  新建能力时必填,修订既有能力时可整段删掉(主 spec 已存在时这一段会被忽略)。
  一到三句,写「这个能力长期解决什么问题」,不写本次改动。
-->
## Purpose
<!-- 本能力长期承担的职责 -->

## ADDED Requirements

### Requirement: [FR-001] <!-- 名词短语:这条管的是什么。不写 MUST。ID 取主 spec 已用最大号 +1 -->

<!-- 主体 --> **MUST** <!-- 行为 -->。
<!-- 下面这行没有就整行删掉 -->
<!-- 主体 --> **MUST NOT** <!-- 禁止的行为 -->。

#### Scenario: <!-- 场景名称 -->
- **WHEN** <!-- 触发条件 -->
- **THEN** <!-- 预期结果 -->
- **判据**:<!-- 怎么一眼看出它成立/不成立。写可观测的东西:某个 grep 无匹配、某字段值、某接口返回码 -->

<!--
  ─────────────────────────────────────────────────────────────────────────
  其余三种 delta 操作的骨架,按需取用。
  用到哪个就把它从本注释里移出来;不用的整段保留在注释里即可(注释内容不参与校验)。
  ─────────────────────────────────────────────────────────────────────────

## MODIFIED Requirements

### Requirement: [FR-0NN] <沿用主 spec 那一条的 ID 与标题>

<主体> **MUST** <更新后的行为>。

ID 与标题都**沿用主 spec 里那一条**(改标题请走 RENAMED,不要在 MODIFIED 里顺手改)。
**必须写出完整的更新后内容**,不能只写差异 —— archive 合并时只保留这里的正文,
写差异会导致细节丢失。
若主 spec 那条还是「标题即整句 MUST」的老写法,可以顺带在这里改成名词短语 ——
但那属于改名,要同时写 RENAMED 段(ID 不变)。

#### Scenario: <场景名称>
- **WHEN** <触发条件>
- **THEN** <预期结果>
- **判据**:<怎么一眼看出它成立/不成立>

## REMOVED Requirements

### Requirement: [FR-0NN] <被移除那条的 ID 与标题>
**Reason**: <为什么移除>
**Migration**: <现有数据 / 调用方如何迁移>

#### Scenario: <场景名称>
- **WHEN** <触发条件>
- **THEN** <预期结果>
- **判据**:<怎么一眼看出它成立/不成立>

## RENAMED Requirements

ID 不变,只换名称 —— 这正是 ID 存在的意义。

- FROM: `### Requirement: [FR-0NN] <旧名称>`
- TO: `### Requirement: [FR-0NN] <新名称>`
-->
