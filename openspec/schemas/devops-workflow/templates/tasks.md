---
title: ""
status: "draft"
updated_at: ""
---

# Tasks

> **本文件是需求的权威源,粒度是「做什么」不是「怎么做」。**
> 判定口径与「为什么」在 `<rules>` 块里(与本模板同一个提示词内),这里只留骨架 —— 不抄第二遍。
>
> 骨架:`- [ ] X.Y 描述(FR-00N)`;实现类任务必须回指需求 ID。
> **本组不涉及就整组删掉**,不要逐条写「无变更」。流水线自身的闸门不写成任务。

## 0. 准备

> 只留机器管不了的。

- [ ] 0.1 确认分支、负责人、发布窗口
- [ ] 0.2 确认测试账号、测试租户、权限点

<!-- openspec:slot task-groups
  【项目特定 · 换项目必须重写本槽内的分组】
  问:本项目的实现任务,按什么顺序分组才能反映「必须先做什么」?
  为什么问:分组顺序本身就暗示了 L4 的分层 —— 排在最前的组对应 Layer 0(串行先做)。
           顺序错了,派生执行计划时容易切错层。
  答案要求:跨模块契约层排在最前(它是并行的前提),其后按 DDD 五层依赖方向排
           (domain/api → application/infrastructure → adapter),再到前端。
           准备 / 测试 / 发布 / 上线后这四组与技术栈无关,保持不变。
           **每组只给一行占位示例,不要预填子项** —— 预填会诱导逐条写「无变更」。

  本项目的权威版本住在 `openspec/rules/enforced/project.md` 的 `TG-N` 条目 —— 改分组先改那里,
  再同步下面标题末尾的 ID。两边 ID 不一致会被 `TEMPLATE/profile-rows` 拦。
-->

## 1. 共享契约层 `weiran-common`(TG-1)

> 对应 `exec/plan.md` 的 Layer 0,**串行先做**,完成后冻结签名。
> 涉及面:跨模块错误码 / 分页契约。改动影响所有依赖 `weiran-common` 的模块。
> **不涉及就整组删掉。**

- [ ] 1.1 <具体改动>(FR-00N)

## 2. 领域层 `*-domain` / `*-api`(TG-2)

> 涉及面:领域模型、端口定义、对外契约类型。**不依赖 Spring,不依赖任何框架**——
> 单测须能在不启动 Spring 的情况下跑。**不涉及就整组删掉。**

- [ ] 2.1 <具体改动>(FR-00N)

## 3. 应用与基础设施层 `*-application` / `*-infrastructure`(TG-3)

> 涉及面:用例编排 / 事务边界(application);MyBatis-Plus / JWT / 密码算法等框架实现
> (infrastructure)。权限点与已知审计缺口(见 CC-5)在这一层落地。
> **不涉及就整组删掉。**

- [ ] 3.1 <具体改动>(FR-00N)

## 4. 适配层 `*-adapter`(TG-4)

> 涉及面:Controller / 认证过滤器。每个模块用 `@AutoConfiguration` 自我登记。
> **不涉及就整组删掉。**

- [ ] 4.1 <具体改动>(FR-00N)

## 5. 前端 `web`(TG-5)

> ⚠️ 本组**没有**对应的 proposal 勾选表,所以提示项保留在这里,动笔前过一遍:
> 页面 / 路由(`App.tsx`)、菜单项(`AdminLayout.tsx`)、数据请求方式、复用组件、
> 权限控制、空态与错误态。每条都要带需求 ID,例如:
> `- [ ] 5.1 角色列表页接入分页组件(FR-005)`。**不涉及就整组删掉。**

- [ ] 5.1 <具体改动>(FR-00N)

<!-- /openspec:slot task-groups -->

## 5. 测试

> 测试跟随实现,不单独拆执行单元(规则 5)。这里列的是**覆盖范围**,不是独立任务。
> 候选:后端单元/接口用例、前端组件/hook 用例、跨模块集成用例、演示模式链路、回归范围。
> 只写本次真会跑的。

- [ ] 5.1 <覆盖范围>

## 6. 发布

> 候选:数据库迁移执行、服务发布、前端发布、配置与环境变量、监控与告警。
> 纯前端改动通常只剩「前端发布」一条 —— 那就只写一条,不要把其余四条写成「无需」。

- [ ] 6.1 <发布动作>

## 7. 上线后

> 候选:验收记录写入 `artifacts.md`、日志/指标样例留存、问题复盘。
> 归档动作(`openspec archive`)是流程闸门,不写成任务。
>
> **`artifacts.md` 不是 openspec artifact**(schema 的 8 个产出物里没有它),也没有机器校验,
> 它的定位是**归档后给人读的验收摘要**。因此**只写 `exec/verify.md` 里没有的东西**:
> 面向人的范围说明、运行时/手工验证的实际操作与结果、已知缺口与未覆盖项。
> **不要复述** L7 证据表、tasks 逐条核对表、结论行 —— 那些在 verify.md 里,复述一遍
> 既不增加信息也会随 verify 的修订而失真;需要时写一句「详见 `exec/verify.md`」即可。

- [ ] 7.1 验收记录写入 `artifacts.md`(只写 verify.md 没有的:人读摘要 / 运行时验证 / 已知缺口)
