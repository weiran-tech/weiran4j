# 密码重置工具专用 profile：只关掉 web 容器，数据源/JWT 等配置仍从叠加的其他 profile
# （本地是 local，见 config/application-local.yml）里读，避免这里重复一份连接串。
#
# 用法：./gradlew :weiran-app:bootRun -Pprofile=local,reset-password
spring:
  main:
    web-application-type: none
