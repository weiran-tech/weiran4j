/**
 * weiran-system 基础设施层：领域端口的具体实现。
 *
 * <p>MyBatis-Plus 实体（{@code *DO}）、Mapper、JWT 与密码算法都关在这一层。
 * 这些类型不允许出现在端口签名上——那是把「换实现」这条自由度换成了编译期依赖。
 */
@NullMarked
package com.weiran.system.infrastructure;

import org.jspecify.annotations.NullMarked;
