/**
 * HTTP 请求体与响应体。
 *
 * <p>只放贴着 HTTP 协议的形状（字段名、校验注解）。跨层传递用 api 层的契约类型，
 * 不要让 Jakarta Validation 注解渗进应用层。
 */
@NullMarked
package com.weiran.system.adapter.web.dto;

import org.jspecify.annotations.NullMarked;
